{
  "type": "function",
  "function": {
    "name": "control_air_circulator",
    "description": "Control the air circulator fan. Call this function whenever user mentions turning on/off the fan or changing speed.",
    "parameters": {
      "type": "object",
      "properties": {
        "device": {
          "type": "string",
          "enum": ["power", "speed", "oscillation", "sound"],
          "description": "Which device feature to control: power (on/off), speed (low/medium/high), oscillation, sound"
        },
        "action": {
          "type": "string",
          "enum": ["turn_on", "turn_off", "low", "medium", "high"],
          "description": "Action: turn_on, turn_off for power; low, medium, high for speed"
        }
      },
      "required": ["device", "action"]
    }
  },
  "server": {
    "url": "https://securevoice-production-eb77.up.railway.app/webhook"
  }
}
