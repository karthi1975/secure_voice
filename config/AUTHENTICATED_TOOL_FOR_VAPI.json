{
  "type": "function",
  "function": {
    "name": "control_air_circulator",
    "description": "Control the air circulator fan. Call this function whenever user mentions turning on/off the fan or changing speed.",
    "parameters": {
      "type": "object",
      "properties": {
        "device": {
          "description": "Which device feature to control: power, speed, oscillation, or sound",
          "type": "string",
          "enum": [
            "power",
            "speed",
            "oscillation",
            "sound"
          ],
          "default": ""
        },
        "action": {
          "description": "Action to perform: turn_on, turn_off, low, medium, or high",
          "type": "string",
          "enum": [
            "turn_on",
            "turn_off",
            "low",
            "medium",
            "high"
          ],
          "default": ""
        }
      },
      "required": [
        "device",
        "action"
      ]
    }
  },
  "server": {
    "url": "https://securevoice-production.up.railway.app/webhook"
  }
}
