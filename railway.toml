[build]
builder = "NIXPACKS"
buildCommand = "cd webhook_service && pip install -r requirements.txt"

[deploy]
startCommand = "cd webhook_service && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
